This is a simple tutorial how basic websocket work 

objective: 
send the message (client) -> received the message to the server -> again server send to the client ->client will recevive it 
c -> s ->c 

task: 
I have generated the client a simple website where there is a form and you can type the message 
this message will be passed to the server with the a get protocol asking to switch the server the protocol to ws (websocket)
then it will give the response that it got upgraded to the websocket the connection is then established 

npm start to start the sever 
then using the live stream extention run the html file 
